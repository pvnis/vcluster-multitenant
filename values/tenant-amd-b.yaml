# Overlay for tenant-amd-b. Applied after tenant.yaml and amd-tenant.yaml:
#   vcluster create tenant-amd-b -n tenant-amd-b \
#     --values tenant.yaml --values amd-tenant.yaml --values tenant-amd-b.yaml
controlPlane:
  service:
    httpsNodePort: 31643
